<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="clanList">
	<select id="list" resultType="ClanList">
		SELECT * FROM clan_list
	</select>
	
	<insert id="add">
		INSERT INTO clan_list (clan_code,clan_name,wol_code,clan_title,clan_content,clan_view,clan_date)
		VALUES (CLAN_LIST_SEQ.NEXTVAL,#{clanName}, #{wolCode}, #{clanTitle}, #{clanContent}, #{clanView}, sysdate)
	</insert>
	
	<select id="item" resultType="ClanList">
		SELECT * FROM clan_list
		WHERE clan_code = #{clanCode} AND clan_name = #{clanName}
	</select>
	
	<update id="update">
		UPDATE clan_list 
		SET wol_code= #{wolCode},clan_title= #{clanTitle},clan_content= #{clanContent},clan_view= #{clanView}
		WHERE clan_code = #{clanCode} AND clan_name = #{clanName}
	</update>
	
	<delete id="delete">
		DELETE FROM clan_list 
		WHERE clan_code = #{clanCode} AND clan_name = #{clanName}
	</delete>
	
<!-- 	<select id="detail" resultType="ClanList"> -->
<!-- 		SELECT * FROM clan_list -->
<!-- 		WHERE clan_code = #{clanCode} AND clan_name = #{clanName} -->
<!-- 	</select> -->
</mapper>