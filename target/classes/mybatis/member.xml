<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<!-- 회원가입 -->
	<insert id="add">
		INSERT INTO member (member_code, id, pw, name, birth, sex, tel)
		VALUES (MEMBER_CODE_SEQ.nextval, #{id}, #{pw}, #{name}, #{birth}, #{sex}, #{tel})
	</insert>
	<!-- 중복확인 -->
	<select id="idCount" parameterType="String" resultType="Integer">
		SELECT COUNT(*) FROM member WHERE id = #{member_id}
	</select>
	<!-- 로그인 -->
	<select id="check" parameterType="HashMap" resultType="member">
		SELECT * FROM member WHERE id=#{id} AND pw=#{pw}
	</select>
</mapper>